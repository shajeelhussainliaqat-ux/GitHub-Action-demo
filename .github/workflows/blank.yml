name: Hello World
on: 
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
    branches: 
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "A demo build"

  
          
